{
  "installCommand": "cd \"CIT MIT backend\" && npm install && cd \"../CIT MIT frontend\" && npm install",
  "buildCommand": "cd \"CIT MIT frontend\" && npm run build",
  "outputDirectory": "CIT MIT frontend/dist",
  "functions": {
    "api/index.js": {
      "includeFiles": "CIT MIT backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" }
  ]
}
